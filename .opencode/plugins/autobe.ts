// Local development entry-point.
// Re-exports the plugin from the package root so both the npm distribution
// and the local .opencode/plugins/ path load exactly the same code.
export { default } from "../../index.ts";
